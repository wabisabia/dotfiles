[core]
  pager = delta

[interactive]
  diffFilter = delta --color-only

[delta]
  navigate = true
  dark = false
  line-numbers = true
  features = gruvbox-dark

[merge]
  conflictstyle = diff3

[diff]
  colorMoved = default
  external = difft

[alias]
  a = add
  b = branch
  bp = remote update origin --prune
  bu = branch --set-upstream-to
  c = commit
  cm = commit -m
  ca = commit --amend
  d = diff
  ds = diff --staged
  f = fetch
  l = log
  ln = log -n
  pl = pull
  ps = push
  r = restore
  rs = restore --staged
  s = status
  sw = switch
  w = worktree
  wa = worktree add
  wl = worktree list
  wr = worktree remove

[user]
	email = chris@wabisabia.com
	name = Christian Dunn
